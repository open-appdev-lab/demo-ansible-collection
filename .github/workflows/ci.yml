name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  ci:
    uses: open-appdev-lab/ansible-collections-ci/.github/workflows/ci-ansible-collection.yml@v1.0.8
    permissions:
      contents: write
      packages: write
    secrets: inherit